# Markdown Tables in Claude Code CLI

Claude Code CLI breaks markdown tables when row width exceeds ~140 characters. To ensure tables render correctly:

- **Keep total row width under ~140 characters** (all columns combined)
- **Use icons + abbreviations** to save space while staying clear (e.g., `✏️ M` instead of "Modified")
- **Shorten content** - use file names not full paths, concise descriptions
- **Prefer more rows over wider rows** - split content if needed

**Common icons:** ✏️ (edit), ➕ (add), 🗑️ (delete), ✅ (success), ❌ (fail), ⚠️ (warning), 🔧 (fix)
